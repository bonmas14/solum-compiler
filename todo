- Rewrite parser to not use BNF-type logic to parse files. because it is too much of a code
- Rewrite logger because it got render bugs again

- fix error where no token info is transfered in case of:
    
    cast(Char*) something;
