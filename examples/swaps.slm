main : () = {
    a, b, c : s32; 
    a, b, c = c, b, a;

    d, e, f := 1, 2, 3;
    g, h, i : s32, s32, u64 = default;
    j, k, l : s32, s32, u64;
}

rand : (a : s32) -> s32 = {
    return a - 19 * a;
}
