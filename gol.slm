// so for game of life we need pointers and arrays

// a, b, c : b32, s32, u64 = default;



Proto : () -> A = prototype;

A : struct = {
    p : Proto;
}

main : (argc : s32, argv : ^^u8) -> s32 = {
    return 0;
}

