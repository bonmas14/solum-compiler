use: "win";

State: struct = {
    w, h:  u64;
    array: ^u8;
}

main: () -> s32 = {
    array: [100, 100]u8;

    state : State;

    state.w = 100;
    state.h = 100;

    state.array = ^array;

    while true {
        step(@state);
        draw(@state);
    }

    return 0;
}

step: (state : ^State) = {

}

draw: (state: ^State) = {
    
}
